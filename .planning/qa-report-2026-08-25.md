# QA report: Results progressive disclosure

Date: 2026-08-25
Branch: `agent/results-progressive-disclosure`
Environment: local production-style frontend/backend, Chromium, locked repository dependencies

## Outcome

The Results hierarchy passed its focused and broad regression checks at phone, tablet, and desktop widths. The default Results view remains concise; Review, Investigation, and Evidence Record deepen in distinct stages; unknown and unauthorized identities fail closed.

## Browser flows verified

| Viewport | Flow | Expected and observed |
|---|---|---|
| 390x844 | Results -> Review -> Investigation -> Evidence | Compact card <=340px; no raw IDs on Results/Review; exact comparison and source signals begin in Investigation; complete technical/audit content appears in Evidence; no horizontal overflow |
| 768x1024 | Same four-depth traversal | Content reflows without clipped actions or horizontal overflow; scope labels remain visible |
| 1440x900 | Same four-depth traversal | Results remains decision-oriented; deeper comparison and audit layouts use the available width without leaking into the primary view |
| 390x844 | Unknown Finding, Investigation, and Evidence routes | Each returns its depth-specific unavailable state and does not expose the requested identity or another finding |
| 1440x960 | Authorized and unauthorized shared-workspace evidence routes | Authorized engineer reaches exact finding evidence; unauthorized workspace receives a closed Evidence Record unavailable state without facility/evidence leakage |
| 390x844 and 1440x900 | Evidence-limited analysis | Calm insufficient-evidence summary, no unsupported finding title or recommended check, no overflow |

Serious and critical axe violations: none across all four primary depths at the phone viewport.

## Screenshot evidence

Twelve full-page screenshots were captured and visually inspected:

- `results-progressive-disclosure/phone/{results,review,investigation,evidence}.png`
- `results-progressive-disclosure/tablet/{results,review,investigation,evidence}.png`
- `results-progressive-disclosure/desktop/{results,review,investigation,evidence}.png`

Root: `.planning/screenshots/`

## Automated verification

- Frontend unit suite: 58 files, 490 tests passed.
- Focused projection/workspace unit suite: 3 files, 39 tests passed after the final evidence restoration.
- Frontend lint: passed with zero warnings.
- Production build: passed.
- Backend evidence contracts: 16 passed (`test_analysis_result_contract.py`, `test_finding_confidence_contract.py`, `test_evidence_package.py`).
- Focused Results progressive-disclosure Chromium suite: 5 passed.
- Results-adjacent regression set: 17 passed after expectation/fixture reconciliation.
- Full Chromium suite: 63 passed, 1 failed.
- `git diff --check`: passed.

## Documented exceptions

1. Full Chromium retains one unrelated failure in `codex-cloud-chilled-water.spec.js`: baseline creation stays on `/workspace/data-sources` and times out before reaching `/baselines/:id/ready`. The failure occurs before the Results experience and reproduced independently of the Results assertions.
2. The aggregate performance gate still fails for Live Monitoring raw bytes and Data Sources raw/gzip bytes. The Engineering workspace now passes its own budget after deep finding workspaces were deferred. No unrelated Live Monitoring or Data Sources implementation/budget was changed.

No deployment, commit, push, merge, or cross-worktree mutation was performed.
