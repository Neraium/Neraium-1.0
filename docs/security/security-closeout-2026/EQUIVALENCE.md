# Final S08 analytical equivalence

PASS. The unchanged existing 10K repeat gate completed two full production executions. Each equals each of the two currently certified closeout outputs under the existing semantic comparator: four direct comparisons, **zero semantic differences**, all analytical groups equal. No new normalization, exclusions or identity bridge.

Semantic digest: `bd70db4d53f6a28d262834e1f7cd5e55a8bf3f6661f0a0c6a17720496ddbf907`.
Result/provenance digest: `e41a27d2c2b953b13321fdc0d54207f75ec76f8c14b9158c8297619d24566aac`.

Evidence: `raw/s08-final/GATE_PREDECLARATION.json`, `gate/GATE.json`, `target-comparison.json`, `target-semantic-mismatches.json`, and retained complete outputs. The existing SOURCE bridge checks also passed without modification. Evidence-store digest plus four provenance links per upload verified; canonical evidence and replay, source/behavioral identity, numerical relationships, ranking/ties, persistence/recurrence, context, sufficiency and Measurable Consequence remain unchanged.

All analytical producer, canonical/provenance/replay and comparator/worker hashes equal the certified baseline. #1/#2/#3/#5 intact (#3 ACCEPT_WITH_LIMITATIONS); #4 absent; zero new Aletheia EVPs and no active gate. Read-only/outside-control-path/non-actuating architecture preserved. Customer failure projection is a separate presentation contract, not a rewrite of raw evidence or an analytical normalization.

27 focused security and 15 analytical tests passed before workload start. The 10K worker gate validates governed production analysis, while separate HTTP tests validate failure presentation/forensic authorization. No production or comparator changes after freeze. No 500K performance measurement; the retained SOURCE 121.610119-second reference remains unreproduced in Neraium-1.0 by this task.


---

## Earlier closeout record (retained historical evidence)

# Analytical equivalence

PASS. Four direct comparisons: each new complete 10K output equals each retained security-certified TARGET output under the unchanged semantic_content contract. No new bridge/exclusion. Zero mismatches; all analytical component groups match. See raw/target-comparison.json and raw/target-semantic-mismatches.json.

Semantic digest: `bd70db4d53f6a28d262834e1f7cd5e55a8bf3f6661f0a0c6a17720496ddbf907`.
Result/provenance digest: `e41a27d2c2b953b13321fdc0d54207f75ec76f8c14b9158c8297619d24566aac`.

Complete governed result, graph/evidence/ranking, exact-tie rule, persistence/recurrence, operating context/sufficiency, Measurable Consequence, state/summaries, behavioral/source identity, provenance and canonical evidence preserved. The unchanged existing gate also passed its retained SOURCE bridge comparisons, evidence-store and four provenance links per run, zero new EVPs and no active Aletheia gate. Targeted regressions cover canonical replay/tampering, runtime separation, affected-signal order, terminal attempt metadata and historical compatibility.

Optimizations #1/#2/#3/#5 unchanged by source hash; #3 ACCEPT_WITH_LIMITATIONS; #4 remains absent. Read-only, outside-control-path, no equipment write-backs/actuation preserved. All analytical producer hashes, comparator, bridge and retained worker unchanged. Security HTTP presentation is covered separately by focused tests; the deterministic gate does not certify every HTTP failure path or production infrastructure.

One gate only. No 500K/1M/LBNL/full suite. Retained SOURCE 121.610119-second 500K reference remains unreproduced in Neraium-1.0 by this task; no new performance claim.
