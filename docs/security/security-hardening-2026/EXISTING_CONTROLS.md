# Verified existing controls

| Area | Repository evidence | Practical scope |
|---|---|---|
| Read-only architecture | docs/sii_read_only_integration_standard.md; backend/runtime/runtime_contracts.py; app/connectors/https_telemetry.py; historian_provider.py | Observation/evidence, not equipment command execution; reviewed database executors must enforce read-only privileges. |
| Authentication | core/security.py:require_api_access; routers/auth.py; services/auth_store.py | Production rejects absent/invalid credentials; password PBKDF2-HMAC-SHA256 (200,000 iterations), random salt, constant-time password hash comparison; random 48-byte-url-safe sessions, expiry/revocation; HttpOnly/Lax/Secure production cookie. No MFA claim. |
| Authorization and isolation | workspace_authorization.py:resolve_workspace_context; telemetry_authorization.py; dataset_scope.py; phase4_scope.py | Active membership or explicit service workspace allowlist; header is selector, not authority; authenticated scope propagated; opaque missing/cross-scope errors. Personal workspaces use actor-derived scope. |
| Uploads/paths | routers/data.py:upload_data and upload-session completion; core/path_safety.py | Generated server names, NamedTemporaryFile, containment and symlink checks, allowed suffixes, chunked file loop/cap, queue limits; object completion validates expected size. Filename is metadata, not storage authority. |
| Telemetry validation | contracts.py; models/telemetry_api_models.py; services/telemetry_ingestion.py | Structured bounded models, query/header constraints, field/count checks; no change to numerical interpretation, missing-data policies or nonfinite analytical handling. |
| Canonical evidence/replay | telemetry_result_artifact.py:build/decode and _validate_payload_identity; telemetry_result_service.py; analysis_provenance.py:result_digest | Canonical JSON/hash, bounded decompression, schema/index/source validation, replay exact stored payload; source-bound result digests and evidence links. Hashes are not signatures against a fully privileged storage attacker. |
| Persistence | runtime_db.py; telemetry_repository.py; auth_store.py | Bound SQL values, scoped queries and transactions. Dynamic identifiers inspected in runtime schema/import code derive from fixed/schema-owned names, not request SQL. No arbitrary client SQL in current historian provider contract. |
| Secrets/egress | telemetry_secrets.py; telemetry_egress.py; connectors/https_telemetry.py | Server-side opaque secret bindings; scoped resolution; HTTPS destination checks, all DNS answers checked, pinning, redirects/environment proxies disabled, request budgets; public API avoids raw credential configuration. |
| Errors/logging | main.py exception handlers; logging_config.py; routers/auth.py | Generic unhandled API errors; validation strips input/ctx; credential pattern/structured-key redaction, correlation IDs; auth failures/admin changes/evidence exports audited. Not arbitrary PII removal; see findings. |
| Rate/resources | rate_limiter.py; routers/auth.py and data.py; telemetry_egress.py | Production login IP/account limits, upload subject buckets, bounded API model/counts and connector response budgets. In-memory limiter is process-local. |
| Browser | React JSX rendering; frontend/src/services/api/authApi.js; datasetSessionCache.js | No dangerouslySetInnerHTML/innerHTML/eval sink found in frontend/src search; server session cookie; per-scope cache clearing. Browser localStorage still retains analysis history/notes and session email. |
| Container/config | Dockerfile, backend/Dockerfile, .dockerignore, core/config.py, .github/workflows/deploy-backend.yml | Non-root UID 10001, explicit COPY, runtime-only writable owned directories, healthcheck; secret/env/runtime exclusions; root image digest pin; runtime/telemetry PostgreSQL TLS validation and managed auth secret references. Configuration is evidence, not proof deployed settings match. |
| Dependencies | backend/requirements.txt; frontend/package-lock.json | Pinned direct Python packages, hash-pinned consequence source; frontend lockfile. No new dependency added in this campaign. |
