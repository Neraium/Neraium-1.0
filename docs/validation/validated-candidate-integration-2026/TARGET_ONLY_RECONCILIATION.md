# Reconciliation under human authority

The earlier conflict inventory is retained [verbatim](history-before-human-authority/TARGET_ONLY_RECONCILIATION.md).

- UTC behavioral observation attribution: target bytes retained.
- Phase 4: explicit source identity/content fallback retained; run/job runtime trace now explicitly producer-marked for current v2 serialization.
- sii_inputs: schema columns followed by sorted extra mapping keys retained; observation row order unchanged.
- Canonical artifact validation: runtime run-ID fallback and strict source equality retained unchanged.
- Telemetry projection: runtime-aware validation retained, SOURCE runtime/version transport allowlist added.
- Preliminary engine runtime trace propagation retained with explicit envelope marking.
- Stable content-derived missing relationship IDs, lexical system prefixes, shared-signal set ordering retained. Removed only their use as analytical rank criteria. Supporting evidence remains analytically ordered.
- TARGET removal of execution-clock source windows retained. No missing source time is fabricated.
- Runtime ID isolation retained for execution producers; source-window/ingestion exceptions explicit. Current version differs from both historical v1 encoders, which remain readable.

Independent SOURCE optimization/retirement hunks are applied through a common-ancestor three-way reconciliation. No blind whole-file source overwrite, benchmark optimization, security hardening or historical artifact rewrite is authorized.

## Post-gate correction

The initial reconciliation incorrectly retained lexical affected-signal ordering in localize_condition and _corroboration_result. The frozen gate exposed its effect on recommendations, evidence reference hashes and consequence source-tag order. Both now preserve SOURCE encounter order. This is enforcement of the supplied architecture, not a new decision or comparator exception.

Current terminal publication added attempt_id after upload encoding. It now re-encodes current-version result metadata after assigning the attempt, preserving terminal summary ownership and runtime-aware retry comparisons. This field was already in the pre-gate runtime schema; no comparator exclusions were added. Legacy records bypass the new encoding branch.

Final current identity aliases are selected by producer/version, not by mutable runtime metadata. Projections carry identity_contract. All four named target-only behaviors remain. The complete gate is not rerun.
