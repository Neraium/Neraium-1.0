# Reproducibility and deployment requirements — review incomplete

Observed local contracts: Python 3.11 in CI and backend image; shared package requires >=3.11. Node 20 in CI; frontend dependencies have package-lock.json and use npm ci. Backend requirements pin direct packages and an immutable/hash-qualified consequence source archive; transitive locking and base-image policy retain prior limitations. Root Dockerfile pins a base digest; backend Dockerfile uses python:3.11-slim. Backend deployment workflow builds with `docker build -f backend/Dockerfile ... backend`; both root and backend contexts have .dockerignore files. Entrypoint is `python -m app.entrypoint`, service port 8080 and /api/health check. Docker copy paths were inspected, but image construction was not run.

Configuration categories to supply through approved deployment configuration/secret management: production/staging APP_ENV; process role and worker/poller settings; runtime directory/permissions; auth/runtime database endpoints and TLS/managed-secret references; bootstrap/service authentication; shared upload/object storage and IAM; explicit CORS/frontend API origin; trusted proxy settings; upload/queue/retention/resource limits; optional telemetry/notification credentials. Never invent production values or put credentials in the release record. Environment templates exist at .env.example and backend/.env.example. frontend/.env.production is tracked configuration and still requires release-specific content review.

Persistence and migrations are startup/repository-managed; deployment backup, migration compatibility and shared-storage readiness review remains pending because preparation stopped. README documents shared PostgreSQL runtime records and S3 upload artifacts, with local SQLite for development. No startup/migration workload or environment change was attempted.

Blocking reproducibility dependency: the ordinary certification inventory test references absent retained semantic snapshots and FIELD_TRACE.json (exact paths in FILE_MANIFEST.json and RELEASE_READINESS.json). Restore authoritative evidence or explicitly authorize a change to the historical test's packaging contract before continuing; do not regenerate or weaken it silently.

Required follow-up after resolving that blocker: finish static secret review; finalize exact release/commit boundary; confirm dependency/runtime versions and migrations; run the minimal permitted build/import checks; recheck certified hashes. No additional analytical or security campaign is implied.
